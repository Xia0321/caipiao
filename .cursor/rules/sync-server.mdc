---
description: 代码修改后默认同步到 Git 与 SSH 服务器（可被用户明确排除）
alwaysApply: true
---

# 代码修改后的默认同步策略

在完成**本地代码修改**后，除非用户**明确说明**不需要，否则必须依次完成以下两步：

## 1. 提交并推送到 Git 远程仓库

- 在项目根目录执行：
  - `git add -A`（或只 add 本次修改涉及的文件）
  - `git commit -m "简短描述本次修改"`
  - `git push`
- 若仓库有未提交修改，先完成提交再推送；若用户未提供 commit 信息，根据本次修改内容写一句简短的 commit message。

## 2. 同步到 SSH 远程服务器

- 在项目根目录执行同步脚本：
  - `Set-Location "F:\newProects\caip"; .\scripts\sync_to_server.ps1`
- 脚本会将本地项目按相对路径同步到：`root@154.219.120.254:/www/wwwroot/154.219.120.254`
- 若提示输入 SSH 密码，由用户在本机终端输入。

## 用户明确排除时

当用户说明**不需要**更新 Git 和/或远程服务器时，则跳过对应步骤。例如：
- 「不需要更新 git 和远程服务器」「不要同步」「只改代码不提交」「不要 push」「不要同步到服务器」等 → 不执行上述 1 和 2。
- 「只提交 git，不要同步服务器」→ 只执行 1，不执行 2。
- 「只同步服务器，不提交 git」→ 只执行 2，不执行 1。

不要将 SSH 密码写在任何文件或规则中。
